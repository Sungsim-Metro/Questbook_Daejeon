# 앱 디렉토리

`apps/`는 사용자에게 보이는 웹 화면을 보관한다. 현재 루트에 있는 정적 MVP 파일은 그대로 유지하고, 새 구현은 아래 앱으로 나누어 추가한다.

- `user-web/`: 일반 사용자 모바일 웹/PWA
- `partner-web/`: 향후 지역 상점 파트너 포털
- `admin-web/`: 향후 운영 관리자 화면

각 앱은 앱 서버의 비밀 값이나 외부 REST API Key를 직접 사용하지 않는다. 필요한 데이터는 웹 게이트웨이를 거쳐 앱 서버 API로 요청한다.
