# 인프라 디렉토리

로컬 개발과 NCP 배포 구성을 보관한다.

- `local/`: 로컬 개발용 실행 구성
- `ncp/`: VPC, subnet, 로드 밸런서, 웹 서버, 앱 서버, DB, NAT Gateway 구성
- `nginx/`: 정적 파일 제공과 `/api` 프록시 설정

설계서 기준 baseline은 웹 서버 1대, 앱 서버 1대, 관리형 관계형 DB 1대, 앱 서버 인메모리 캐시이다. 앱 서버가 2대 이상으로 늘어날 때 Redis 같은 외부 캐시를 검토한다.
