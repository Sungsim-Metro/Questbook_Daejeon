# 부트스트랩 전용 최소권한 Sub Account API 키.
# 권한: Container Registry pull + 아래 시크릿 읽기만. 루트 계정 키 사용 금지.
NCP_API_ACCESS_KEY=
NCP_API_SECRET_KEY=
# 배포 설정 시크릿(내용: REGISTRY, IMAGE_TAG)의 secretId. 모든 VM 공통.
QBOOK_DEPLOY_SECRET_ID=
# 앱 시크릿(app.env 내용)의 secretId. app VM만 채우고 web VM은 비워둔다.
QBOOK_APP_SECRET_ID=
